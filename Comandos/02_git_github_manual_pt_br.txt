=== 02. GIT & GITHUB MASTER ===
1. WORKFLOW
git init
git clone <url>
git status
git add .
git commit -m "feat: alteracao"
git push origin main
git pull --rebase origin main

2. BRANCHES & REVERT
git checkout -b feature/nova
git merge feature/nova
git stash / git stash pop
git reset --soft HEAD~1