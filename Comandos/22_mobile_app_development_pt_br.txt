=== 22. DESENVOLVIMENTO MOBILE (FLUTTER E REACT NATIVE) ===
1. FLUTTER (DART)
import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('App Seguro')),
        body: Center(child: Text('Olá Mundo Mobile')),
      ),
    );
  }
}

2. SEGURANÇA EM APPS MÓVEIS
- SSL Certificate Pinning.
- Armazenamento Seguro (Flutter Secure Storage / Keychain / KeyStore).
- Ofuscação de código no build de produção.